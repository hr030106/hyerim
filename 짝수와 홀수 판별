#include <stdio.h>

int main() {
    int num;
    printf("숫자를 입력하세요: ");
    scanf("%d", &num);
    
    if (num % 2 == 0) {
        printf("입력한 숫자는 짝수입니다.\n");
    } else {
        printf("입력한 숫자는 홀수입니다.\n");
    }
    return 0;
}
