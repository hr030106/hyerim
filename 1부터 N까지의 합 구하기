#include <stdio.h>

int main() {
    int N, sum = 0;
    printf("N 값을 입력하세요: ");
    scanf("%d", &N);

    for (int i = 1; i <= N; i++) {
        sum += i;
    }

    printf("1부터 %d까지의 합은 %d입니다.\n", N, sum);
    return 0;
}
